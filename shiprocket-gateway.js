const http=require('http');const fs=require('fs');const path=require('path');const crypto=require('crypto');const{createPrepaidOrder,configured}=require('./shiprocket-service');const{handleCustomerOrders}=require('./customer-orders-service');require('dotenv').config();
const publicPort=Number(process.env.PORT||3000);let ordersPort=Number(process.env.SHIPROCKET_ORDERS_GATEWAY_PORT||3301);const storePort=Number(process.env.ROYALWRAP_INTERNAL_PORT||3101);if(ordersPort===public