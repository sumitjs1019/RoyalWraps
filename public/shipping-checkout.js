(()=>{function installFields(){const form=document.getElementById('checkoutForm');if(!form||form.querySelector('[name="city"]'))return;const pincode=form.querySelector('[name="pincode"]')?.closest('label');if(!pincode)return;const city=document.createElement('label');city.innerHTML='City<input type="text" name="city" placeholder="Enter city" required minlength="2" autocomplete="address-level2">';const state=document.createElement('label');state.innerHTML='State<input type="text" name="state" placeholder="Enter state" required minlength="2" autocomplete="address-level1">';pincode.after(city,state)}
const originalFetch=window.fetch.bind(window);window.fetch=(input,init={})=>{const url=typeof input==='string'?input:input?.url;if(url==='/api/create-order'&&typeof init.body==='string'){try{const payload=JSON.parse(init.body),form=document.getElementById('checkoutForm'),data=form?new FormData(form):null;payload.customer={...(payload.customer||{}),city:String(data?.get('city')||'').trim(),state:String(data?.get('state')||'').trim()};init={...init,body:JSON.stringify(payload)}}catch{}}return originalFetch(input,init)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',installFields);else installFields();})();
